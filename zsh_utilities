#
# Import utility functions
#

zshUtilsPath=${zshConfigPath}/utils
[ -d ${zshUtilsPath} ] && {
    pushd ${zshUtilsPath} >/dev/null
    for util in $(/bin/ls -1 $(pwd)); do
	source ${util}
    done
    popd >/dev/null
}
